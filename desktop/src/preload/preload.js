// Preload script - No contextBridge needed because nodeIntegration is enabled
// The renderer can directly use require('electron')
// This file is kept for future use if security requirements change
